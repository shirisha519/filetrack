HomePage.js
import React from "react";
import HomePage from "./HomePage";
import { Outlet } from "react-router-dom";

const Layout = () => {
  return (
    <div>
      <HomePage /> {/* Header + Navbar + Sidebar */}
      <div className="page-content"> 
        <Outlet /> {/* Routed pages render here */}
      </div>
    </div>
  );
};

export default Layout;

Layout.js
import React from "react";
import HomePage from "./HomePage";
import { Outlet } from "react-router-dom";

const Layout = () => {
  return (
    <div>
      <HomePage /> {/* Header + Navbar + Sidebar */}
      <div className="page-content"> 
        <Outlet /> {/* Routed pages render here */}
      </div>
    </div>
  );
};

export default Layout;


App.js



import Layout from "./components/Layout";
import React, { useState } from "react";
import { Routes, Route, Navigate } from "react-router-dom";
import LoginPage from "./components/LoginPage";
import HomePage from "./components/HomePage";
import Grievance from "./pages/Grievance";
import PaySlip from "./pages/PaySlip";
import SrConfirm from "./pages/SrConfirm";
import PrExp from "./pages/PrExp";
import AppeRevision from "./pages/AppeRevision";
import Sick from "./pages/Sick";
import SickReport from "./pages/SickReport";
import Attendance from "./pages/Attendance";
import Annual from "./pages/Annual";
import Residential from "./pages/Residential";
import ApplyLeave from "./pages/ApplyLeave";
import EmpRelieving from "./pages/EmpRelieving";
import Vaccination from "./pages/Vaccination";
import AttendanceMark from "./pages/AttendanceMark";



function App() {
  const [isAuthenticated, setIsAuthenticated] = useState(false);

  return (
    <Routes>
      <Route path="/" element={<LoginPage setIsAuthenticated={setIsAuthenticated} />}/>
      <Route path="/home" element={ isAuthenticated ? (
            <HomePage setIsAuthenticated={setIsAuthenticated} />
          ) : (
            <Navigate to="/" />
          ) }/>
          <Route path="/" element={<Layout />} />
          <Route path="/sr-confirm" element={<SrConfirm />} />
         <Route path="/grievance" element={<Grievance />} />
        <Route path="/pay-slip" element={<PaySlip />} />
        <Route path="/pr-explnation" element={<PrExp />} />
        <Route path="/appeal-revision" element={<AppeRevision />} />
        <Route path="/sick" element={<Sick />} />
        <Route path="/sick-reporting" element={<SickReport />} />
        <Route path="/attendance" element={<Attendance />} />
        <Route path="/annual" element={<Annual />} />
        <Route path="/residential" element={<Residential/>} />
        <Route path="/apply-leave" element={<ApplyLeave />} />
         <Route path="/employee-relieving" element={<EmpRelieving />} />
          <Route path="/vaccination" element={<Vaccination />} />
           <Route path="/attendanc-mark" element={<AttendanceMark />} />

    </Routes>
  );
}

export default App;
 

i want to see header and navbar in every page like SrConfirm,ApplyLeave
homepage.css
/* === GENERAL PAGE LAYOUT === */

.home {
  background: #e9edf2;
  min-height: 100vh;
  font-family: "Segoe UI", sans-serif;
  display: flex;
  flex-direction: column;
  text-align: center;
  gap: 2px;
}

/* === HEADER SECTION === */
.home-header {
 position: fixed;        /* FIX HEADER */
  top: 0;
  left: 0;
  width: 100%;
  background: #003366;    /* Dark Blue */
  color: white;
  height: 70px;
  padding: 0 20px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  z-index: 1000;         /* Ensure it stays on top */
  border-bottom: 3px solid #002244;
  
}

.logo {
  width: 55px;
  height: 55px;
  margin-right: 15px;
}

.home-header h2 {
  font-size: 22px;
  font-weight: bold;
}

/* === NAVBAR === */
.navbar {
   position: fixed;        /* FIX NAVBAR */
  top: 70px;              /* Below header */
  left: 0;
  width: 100%;
  background: #0055aa;
  padding: 10px 20px;
  display: flex;
  gap: 30px;
  align-items: center;
  z-index: 999; 
    
}

.navbar a,
.navbar span {
  color: white;
  text-decoration: none;
  cursor: pointer;
  font-size: 15px;
}
.navbar a {
  color: white;
  text-decoration: none;
  font-weight: bold;
}

/* === DROPDOWN MENUS === */
.dropdown {
  position: relative;
  display: inline-block;
}

.dropdown > a {
  cursor: pointer;
  font-weight: 600;
  transition: color 0.3s ease;
}

.dropdown > a:hover {
  color: #ffd700;
}

.dropdown-content {
  display: none;
  position: absolute;
  background-color: #ffffff;
  min-width: 260px;
  box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.2);
  border-radius: 6px;
  z-index: 999;
  text-align: left;
}

.dropdown-content a {
  color: #002b5c;
  padding: 10px 16px;
  text-decoration: none;
  display: block;
  font-weight: 500;
  transition: background-color 0.3s;
}

.dropdown-content a:hover {
  background-color: #e6f0ff;
}

.dropdown:hover .dropdown-content {
  display: block;
}

/* === PROFILE SECTION IN NAVBAR (RIGHT SIDE) === */
.profile-container {
   display: flex;
  align-items: center;
  gap: 8px;
  cursor: pointer;
  margin-left: auto;
}

.profile-img {
  width: 42px;
  height: 42px;
  border-radius: 50%;
  border: 2px solid white;
  margin-right: 8px;
  object-fit: cover;
}

.profile-name {
  color: white;
  font-weight: 600;
  font-size: 15px;
}

/* === PROFILE DROPDOWN === */
.profile-dropdown {
  position: absolute;
  right: 0;
  top: 55px;
  background: white;
  border-radius: 8px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
  min-width: 230px;
  z-index: 999;
  animation: fadeIn 0.25s ease-in-out;
}

.profile-dropdown a {
  display: block;
  padding: 10px 16px;
  color: #002b5c;
  text-decoration: none;
  font-weight: 500;
}

.profile-dropdown a:hover {
  background-color: #e6f0ff;
}

.profile-dropdown hr {
  border: none;
  border-top: 1px solid #ddd;
  margin: 5px 0;
}

.profile-header {
  padding: 10px 16px;
  text-align: left;
  color: #002b5c;
}

.profile-header strong {
  font-size: 16px;
  display: block;
}

.profile-header p {
  font-size: 13px;
  color: #555;
  margin: 4px 0 0;
}

/* Smooth dropdown fade effect */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(-5px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* === MAIN CONTENT AREA === */
.main-section {
 display: flex;
  padding: 20px;
  gap: 20px;
  flex: 1;

  margin-top: 140px; 
}
/* SUCCESS STORIES */
.success-stories {
  background: #0f2853;
  color: white;
  padding: 12px 15px;
  border-radius: 6px;
  margin-bottom: 18px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.view-more-btn {
  background: #0ab36c;
  border: none;
  padding: 6px 14px;
  border-radius: 4px;
  cursor: pointer;
  color: white;
}
/* NOTIFICATIONS */
.notifications-box {
  background: #e7edf7;
  border-left: 4px solid #2c3971;
  padding: 14px 16px;
  margin-bottom: 25px;
  border-radius: 6px;
  font-size: 14px;
  color: #2b3655;
  line-height: 1.4;
  position: relative;
}
.notifications-btn {
  background: #2c3971;
  color: white;
  border: none;
  padding: 6px 12px;
  border-radius: 5px;
  cursor: pointer;
  font-size: 13px;
  position: absolute;
  right: 14px;
  bottom: 10px;
}
/* PERSONAL SERVICES */
.section-title {
  font-size: 18px;
  font-weight: bold;
  margin-bottom: 15px;
  color: #003366;
  border-left: 6px solid #003366;
  padding-left: 8px;
  padding-top: 4px;
}

.service-grid {
  display: grid;
  grid-template-columns: repeat(3, 240px);
  gap: 18px;
  justify-content: flex-start;
}

.service-card {
  background: white;
  border: 1px solid #d4d4d4;
  border-radius: 4px;
  height: 120px;
  
   row-gap: 35px;
  position: relative;
  box-shadow: 0px 1px 3px rgba(0,0,0,0.2);
  padding-top: 45px;
  text-align: center;
  cursor: pointer;
  transition: 0.3s ease;
}

.service-card:hover {
  transform: translateY(-3px);
}

.service-card.large {
  width: 505px !important;
}

.icon {
  width: 45px;
  height: 45px;
  position: absolute;
  top: -20px;
  left: 20px;
  border-radius: 4px;
}

/* Icon Colors */
.green { background: #00796B; }
.blue { background: #0A5E91; }
.gray { background: #616161; }
.teal { background: #0077B6; }
.darkGreen { background: #2E7D32; }
.purple { background: #7B1FA2; }
.maroon { background: #6A1B1A; }

.service-card h4 {
  font-size: 17px;
  color: #003366;
  margin-bottom: 8px;
}

.details {
  background: #2F2B69;
  color: white;
  font-size: 13px;
  width: 100%;
  padding: 6px 0;
  position: absolute;
  bottom: 0;
}

.details p {
  margin: 2px 0;
}

.details.empty {
  background: #2F2B69;
  height: 30px;
}




.side-panel {
  width: 25%;
}

.profile-box {
  background: white;
  padding: 18px;
  border-radius: 6px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
  text-align: center;
}

.side-profile-img {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  border: 2px solid #004b8b;
  margin-bottom: 10px;
}

.profile-title {
  font-size: 20px;
  font-weight: bold;
  margin-bottom: 4px;
}

.profile-sub {
  margin: 2px 0;
  font-size: 14px;
}

.badge {
  padding: 8px;
  border-radius: 5px;
  margin-top: 6px;
  font-weight: bold;
  color: white;
}

/* Badge Colors */
.red { background: #b30b00; }
.green { background: #007b2d; }
.blue { background: #004aad; }
.grey { background: #6b6b6b; }
.orange { background: #d96a00; }
.dark-green { background: #005c2a; }


.content-panel {
  flex: 1;
  background: white;
  padding: 20px;
  border-radius: 6px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
}

/* === DASHBOARD CARDS === */
.cards {
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
  margin-top: 20px;
}

.card {
  background: #004b8b;
  color: white;
  flex: 0 0 5%;
  padding: 15px;
  border-radius: 3px;
  text-align: center;
  transition: 0.3s;
}

.card:hover {
  background: #0067b0;
  transform: translateY(-3px);
}

/* === RESPONSIVE DESIGN === */
@media (max-width: 900px) {
  .navbar {
    flex-direction: column;
    gap: 10px;
  }

  .main-section {
    flex-direction: column;
  }

  .side-panel {
    width: 100%;
  }

  .cards {
    flex-direction: column;
  }
}







import Layout from "./components/Layout";
import React, { useState } from "react";
import { Routes, Route, Navigate } from "react-router-dom";
import LoginPage from "./components/LoginPage";
import HomePage from "./components/HomePage";
import Grievance from "./pages/Grievance";
import PaySlip from "./pages/PaySlip";
import SrConfirm from "./pages/SrConfirm";
import PrExp from "./pages/PrExp";
import AppeRevision from "./pages/AppeRevision";
import Sick from "./pages/Sick";
import SickReport from "./pages/SickReport";
import Attendance from "./pages/Attendance";
import Annual from "./pages/Annual";
import Residential from "./pages/Residential";
import ApplyLeave from "./pages/ApplyLeave";
import EmpRelieving from "./pages/EmpRelieving";
import Vaccination from "./pages/Vaccination";
import AttendanceMark from "./pages/AttendanceMark";



function App() {
  const [isAuthenticated, setIsAuthenticated] = useState(false);

  return (
    <Routes>
      <Route path="/" element={<LoginPage setIsAuthenticated={setIsAuthenticated} />}/>
     <Route element={isAuthenticated ? <Layout /> : <Navigate to="/" />}>
      <Route path="/home" element={<HomePage />} />
          <Route path="sr-confirm" element={<SrConfirm />} />
         <Route path="/grievance" element={<Grievance />} />
        <Route path="/pay-slip" element={<PaySlip />} />
        <Route path="/pr-explnation" element={<PrExp />} />
        <Route path="/appeal-revision" element={<AppeRevision />} />
        <Route path="/sick" element={<Sick />} />
        <Route path="/sick-reporting" element={<SickReport />} />
        <Route path="/attendance" element={<Attendance />} />
        <Route path="/annual" element={<Annual />} />
        <Route path="/residential" element={<Residential/>} />
        <Route path="/apply-leave" element={<ApplyLeave />} />
         <Route path="/employee-relieving" element={<EmpRelieving />} />
          <Route path="/vaccination" element={<Vaccination />} />
           <Route path="/attendanc-mark" element={<AttendanceMark />} />
      </Route>
    </Routes>
  );
}

export default App;
